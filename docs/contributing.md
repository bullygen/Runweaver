# Contributing and releases

See the root contributing guide for environment and checks. Architecture
changes require an ADR with context, decision, alternatives, consequences and
status.

Releases follow Semantic Versioning, install the wheel in a clean environment,
build docs with warnings as errors, execute tutorials and document schema
migrations. Public deprecations remain for at least one minor release.

See the root changelog for release history.
